# Core backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Real-mode AI engine
anthropic>=0.49

# Optional: live Gmail mode (install only if you set AIEA_MODE=live)
# google-api-python-client>=2.0
# google-auth-httplib2>=0.2
# google-auth-oauthlib>=1.2
